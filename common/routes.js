import App from '../containers/App';
import About from '../containers/About';

const routes = [ {
    path: '/',
    component: App
}, {
    path: '/about',
    component: About
} ];

export default routes;
